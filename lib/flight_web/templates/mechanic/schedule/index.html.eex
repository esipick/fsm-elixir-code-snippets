<%=
  render(FlightWeb.Shared.ScheduleView, "index.html",
    conn: @conn,
    renters: @renters,
    instructors: @instructors,
    mechanics: @mechanics,
    aircrafts: @aircrafts,
    instructor_user_id: nil,
    simulators: @simulators,
    rooms: @rooms,
    types: @types,
    instructor_times: @instructor_times,
    mechanic_user_id: @mechanic_user_id,
    student_id: nil)
%>
