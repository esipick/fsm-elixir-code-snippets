defmodule Flight.S3 do
  # def signed_url(url, method, headers \\ %{}) do
  # AWSAuth.sign_url(
  #   Application.get_env(:ohi, :aws_credentials)[:access_key],
  #   Application.get_env(:ohi, :aws_credentials)[:secret_key],
  #   method,
  #   url,
  #   Application.get_env(:ohi, :aws_config)[:region],
  #   "s3",
  #   headers
  # )
  # end
end
